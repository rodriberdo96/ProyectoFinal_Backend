class ProductDto {
    constructor(timestamp, name, description, code, url, price, stock) {
        this.timestamp = timestamp,
        this.name = name,
        this.description = description,
        this.code = code,
        this.url = url,
        this.price = price,
        this.stock = stock
    }
}